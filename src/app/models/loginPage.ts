export interface registerModel {
    firstName: string;
    lastName: string
    userName: string
    email: string
    passWord: string
    role: string
}

export interface loginModel {
    userName: string;
    passWord: string;
}